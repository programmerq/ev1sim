{
  "//": "Config wrapper for the coastdown drag experiment.",
  "//1": "Until 2026-08-12 there was no wrapper, so the run condition behind every",
  "//2": "coastdown number in docs/ev1_chrono_audit.md sections 11 and 11.1 was",
  "//3": "never committed and none of them could be re-derived.  Worse, the",
  "//4": "invocation those sections documented — bare --scenario flags, no",
  "//5": "--config — does one of TWO different things depending on the working",
  "//6": "directory it is run from, and says nothing about which:",
  "//7": "",
  "//8": "  From a directory where config/default.json resolves, it loads the",
  "//9": "  MILFORD level.  The car leaves that level partway through the launch",
  "//10": "  and the solver diverges: peak 370.78 m/s (829 mph), 562 samples above",
  "//11": "  60 m/s, 254 above 200 m/s, speed sign-flipping every few frames.",
  "//12": "",
  "//13": "  From anywhere else, Config.cpp prints 'Cannot open",
  "//14": "  config/default.json — using built-in defaults' to STDERR and falls",
  "//15": "  back to a rigid_plane, which produces a perfectly ordinary coastdown",
  "//16": "  topping out at 30.15 m/s.",
  "//17": "",
  "//18": "Same command line, either outcome, decided by cwd alone — and the only",
  "//19": "signal is one stderr line that a stdout redirect hides.  A silent",
  "//20": "fallback that yields PLAUSIBLE results is worse than one that yields",
  "//21": "garbage: the garbage gets noticed.  That is the likeliest reason this",
  "//22": "sat unexamined for four months.  abs_hard_brake had the same missing",
  "//23": "wrapper, fixed the same day: the runs nobody can repeat are the runs",
  "//24": "nobody wrote a wrapper for.",
  "//25": "",
  "//26": "A uniform rigid plane, long enough for the whole experiment: the launch",
  "//27": "to 30 m/s takes ~380 m and the 75 s coast adds ~1400 m, so 6 km of",
  "//28": "runway leaves the car on the surface for the entire run.  external_sim",
  "//29": "is off — the coastdown is a plant measurement and needs no controllers,",
  "//30": "which is also what makes it runnable from this repo alone.",
  "//31": "",
  "//32": "  ./build/ev1sim --config config/coastdown.json",
  "//33": "  scripts/fit_coastdown.py scenario_coastdown.csv",
  "//34": "",
  "//35": "Naming the config is also what makes the run cwd-independent — but",
  "//36": "only since 2026-08-12.  Config::LoadFromFile warned and returned",
  "//37": "built-in defaults for ANY unreadable path, named or not, so passing",
  "//38": "--config with a typo silently ran a different experiment exactly the",
  "//39": "way the absent default.json did.  src/main.cpp now makes a NAMED",
  "//40": "--config that cannot be opened fatal (exit 2); the implicit default",
  "//41": "still falls back, which is documented behaviour.  Pinned by the",
  "//42": "named_config_is_fatal ctest.",
  "//43": "",
  "//44": "Comparing a before and an after: run it twice, keep both CSVs, and pass",
  "//45": "BOTH to fit_coastdown.py.  It fits them over the speed range they share",
  "//46": "and prints it; handing it one at a time and eyeballing the two outputs",
  "//47": "is how the retracted CdA collapse happened.  See the script header.",

  "vehicle_model": "ev1",
  "terrain": {
    "type": "rigid_plane",
    "surface": "asphalt",
    "friction": 0.9,
    "length_m": 6000.0,
    "width_m": 200.0
  },
  "simulation": {
    "step_size_s": 0.001,
    "render_fps": 60,
    "realtime": false,
    "headless": true
  },
  "telemetry": { "log_to_file": false, "show_hud": false },
  "external_sim": { "enabled": false },
  "vehicle_dynamics": { "start_propulsion_enabled": true },
  "scenario": { "path": "config/scenarios/coastdown.json" }
}
