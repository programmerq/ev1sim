// CI STUB — see protocol.hpp.  The types are header-only (no out-of-line
// symbols), so this TU is intentionally empty; it exists because ev1sim's
// CMakeLists builds electricsim_connector from the four src/io/*.cpp sources
// and checks that each is present.
#include "protocol.hpp"
