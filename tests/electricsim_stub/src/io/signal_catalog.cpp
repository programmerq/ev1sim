// CI STUB — placeholder.  The ev1sim connector references no signal_catalog
// symbols, so this TU is empty; it exists only because ev1sim's CMakeLists
// builds electricsim_connector from the four src/io/*.cpp sources and checks
// that each is present.
