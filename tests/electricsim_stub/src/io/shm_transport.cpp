// CI STUB — see shm_transport.hpp.  Header-only no-op transport; this TU is
// intentionally empty (present only to satisfy the four-source electricsim
// connector build in ev1sim's CMakeLists).
#include "shm_transport.hpp"
